---
- name: "Create Redis StatefulSet"
  kubernetes.core.k8s:
    state: present
    namespace: "{{ redis_k8s_namespace }}"
    definition: "{{ lookup('template', item) | from_yaml }}"
  loop:
    - "01-statefulset.yml.j2"
