---
apiVersion: v1
kind: Service
metadata:
  name: "{{ redis_app_name }}"
  namespace: "{{ redis_namespace }}"
spec:
  type: ClusterIP
  selector:
    app: "{{ redis_app_name }}"
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
